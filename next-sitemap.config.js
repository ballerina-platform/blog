/** @type {import('next-sitemap').IConfig} */

const config = {
    siteUrl: 'https://blog.ballerina.io/',
    generateRobotsTxt: false, // (optional)
    sitemapSize: 5000,
    generateIndexSitemap: false,
}
  
module.exports = config;
  